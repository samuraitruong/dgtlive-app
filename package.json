{
  "name": "dgtlive-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pnpm --stream -r start:prod",
    "start:dev": "pnpm --parallel --stream -r run start:dev",
    "build": "pnpm --parallel --stream -r run build",
    "build-publish":"pnpm --parallel --stream -r run build-publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}
